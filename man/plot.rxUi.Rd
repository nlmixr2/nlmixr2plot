% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odeDiagram.R
\name{plot.rxUi}
\alias{plot.rxUi}
\title{Plot an rxode2 model as an ODE block diagram}
\usage{
\method{plot}{rxUi}(x, y, ...)
}
\arguments{
\item{x}{an rxode2 model function, \code{rxUi} object or nlmixr2 fit}

\item{y}{ignored, for the \code{plot()} generic}

\item{...}{ignored}
}
\value{
a 'DiagrammeR' \code{grViz} htmlwidget
}
\description{
\code{plot()} on an rxode2 user interface object (\code{rxUi}) draws the model's
ordinary differential equations as a block diagram; see \code{\link[=odeDiagram]{odeDiagram()}} for
details.
}
\examples{
mod <- function() {
  ini({
    kin <- 1
    kout <- 0.1
    cl <- 1
    v <- 10
    add.sd <- 0.1
  })
  model({
    d/dt(center) <- -cl / v * center
    cp <- center / v
    d/dt(eff) <- kin - kout * (1 + cp) * eff
    eff(0) <- kin / kout
    eff ~ add(add.sd)
  })
}
if (requireNamespace("DiagrammeR", quietly = TRUE)) {
  plot(rxode2::rxode2(mod))
}
}
