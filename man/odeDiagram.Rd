% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odeDiagram.R
\name{odeDiagram}
\alias{odeDiagram}
\alias{plot.odeBlockDiagram}
\title{Build a block diagram of an rxode2 ODE model}
\usage{
odeDiagram(x, doses = NULL, showZeroIni = FALSE, ...)

\method{plot}{odeBlockDiagram}(x, y, ...)
}
\arguments{
\item{x}{an rxode2 model function, \code{rxUi} object or nlmixr2 fit}

\item{doses}{character vector of states that receive doses.  By default
the first state plus every state with a bioavailability, lag time, rate
or duration.  Use \code{character(0)} to hide dosing.}

\item{showZeroIni}{show \code{state(0) = 0} on integrators without an explicit
initial condition}

\item{...}{ignored}

\item{y}{ignored, for the \code{plot()} generic}
}
\value{
an \code{odeBlockDiagram} object (a list of \code{nodes} and \code{edges}
data frames); use \code{plot()} to render it with 'DiagrammeR' or
\code{as.character()} to get the Graphviz DOT source
}
\description{
Converts the ordinary differential equations of an rxode2/nlmixr2 model
into a control-systems style block diagram.  Every state gets an integrator
(\eqn{1/s}) fed by a summing junction; the additive terms of \code{d/dt(state)}
become the inputs of that junction:
}
\details{
\itemize{
\item terms linear in a single state become gain blocks (triangles) tapped from
that state's integrator output (feedback when the state is the one being
integrated),
\item terms that do not depend on any state become external inputs,
\item any other term becomes a nonlinear function block fed by every state it
depends on (including dependencies through intermediate variables).
}

Dosing is drawn as an input into the dosed compartment's summing junction
and annotated with the model's lag time (\code{alag()}/\code{lag()}), bioavailability
(\code{f()}/\code{F()}) and modeled infusion rate (\code{rate()}) or duration (\code{dur()}).
Initial conditions (\code{state(0)}) are shown on the integrator and model
outputs (endpoints of the residual error model) are drawn on the right.
}
\examples{
one.compartment <- function() {
  ini({
    tka <- 0.45
    tcl <- 1
    tv <- 3.45
    add.sd <- 0.7
  })
  model({
    ka <- exp(tka)
    cl <- exp(tcl)
    v <- exp(tv)
    d/dt(depot) <- -ka * depot
    d/dt(center) <- ka * depot - cl / v * center
    f(depot) <- 0.8
    alag(depot) <- 0.5
    cp <- center / v
    cp ~ add(add.sd)
  })
}

d <- odeDiagram(one.compartment)
d

if (requireNamespace("DiagrammeR", quietly = TRUE)) {
  plot(d)
  # or directly from the model
  plot(rxode2::rxode2(one.compartment))
}
}
\author{
Matthew L. Fidler
}
