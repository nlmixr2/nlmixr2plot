% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelDiagram.R
\name{modelGraph}
\alias{modelGraph}
\title{Build a compartment graph from a model's differential equations}
\usage{
modelGraph(object, dosing = NULL, data = NULL)
}
\arguments{
\item{object}{model to diagram: a model function, an \code{rxode2} user
interface (\code{rxUi}) object, a compiled \code{rxode2} model or a fitted
\code{nlmixr2} object.}

\item{dosing}{optional character vector naming the dosing compartments.
When \code{NULL} the dosing compartments are detected from the dosing records
in \code{data}; when there is no data the first compartment (the default
\code{rxode2} dosing compartment) is used.}

\item{data}{optional dataset used to detect the dosing compartments (from
the dosing records' \code{cmt}).  For fitted models this defaults to the data
the model was fit with.}
}
\value{
a \code{nlmixr2ModelGraph} object; a list with:
\itemize{
\item \code{nodes}: data frame with the compartment \code{name}, its \code{role}
(\code{"dosing"}, \code{"central"}, \code{"peripheral"}, \code{"transit"}, \code{"metabolite"},
\code{"effect"} or \code{"other"}), whether it is \code{dosing} and the layout
coordinates \code{x} and \code{y}.
\item \code{edges}: data frame with \code{from}, \code{to} (\code{NA} for inputs/eliminations),
\code{type} (\code{"transfer"}, \code{"elimination"}, \code{"input"} or \code{"interaction"}),
\code{sign} (\code{1} when the term is added, \code{-1} when it is subtracted; for
interactions \code{1} is stimulation, \code{-1} inhibition and \code{0} an effect whose
direction cannot be determined from the equations, e.g. through
\code{ifelse()} or a conditionally assigned variable),
\code{bidirectional} (for transfers) and \code{label} (the model term(s)).
}
}
\description{
The differential equations are parsed into additive terms.  A term that
is subtracted from one compartment and added (identically) to another is
mass transfer between the two compartments.  A remaining subtracted term
that contains the compartment's own amount is an elimination (output); a
remaining term that depends on other compartments is an interaction that
does not transport mass (for example an effect compartment or a
pharmacodynamic stimulation/inhibition); a remaining added term that does
not depend on any other compartment is a (zero-order) input.
Dependencies through intermediate variables (like \code{cp <- central/v}) are
followed.  A compartment that drives a transfer between two other
compartments (like an enzyme) is drawn as an interaction with the
destination.  Production or loss driven only by another compartment
(like \code{ke0*cp} in an effect compartment) is represented by the interaction
arrow alone, without a separate input/output arrow.
}
\details{
Some limitations on how equations must be written:
\itemize{
\item Mass transfer is only detected when the same term (up to reordering of
the factors of a product) is subtracted from the source and added to the
destination, e.g. \code{d/dt(depot) <- -ka*depot} and
\code{d/dt(central) <- ka*depot - ...}.  Scaled transfer (like a
stoichiometric or volume conversion in only one of the equations) is
shown as an elimination plus an interaction.
\item \code{linCmt()} models are converted to ODEs with \code{rxode2::linToOde()},
which requires a version of 'rxode2' that provides it.
}
}
\examples{
\donttest{
one.cmt <- function() {
  ini({
    tka <- 0.45
    tcl <- 1
    tv <- 3.45
    add.sd <- 0.7
  })
  model({
    ka <- exp(tka)
    cl <- exp(tcl)
    v <- exp(tv)
    d/dt(depot) <- -ka * depot
    d/dt(central) <- ka * depot - cl / v * central
    cp <- central / v
    cp ~ add(add.sd)
  })
}
modelGraph(one.cmt)
}
}
\seealso{
Other model diagrams:
\code{\link[=modelDiagram]{modelDiagram()}}
}
\author{
Matthew L. Fidler
}
\concept{model diagrams}
